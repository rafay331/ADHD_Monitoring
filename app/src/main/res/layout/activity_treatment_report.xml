<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="@color/white">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Write Treatment Report"
        android:textStyle="bold"
        android:textSize="18sp"
        android:layout_marginBottom="16dp" />

    <!-- Treatment Note -->
    <EditText
        android:id="@+id/editNote"
        android:layout_width="match_parent"
        android:layout_height="160dp"
        android:hint="Enter treatment note here..."
        android:gravity="top"
        android:padding="12dp"
        android:textSize="16sp" />

    <!-- ADHD Coping Plan (NEW) -->
    <EditText
        android:id="@+id/editCopingPlan"
        android:layout_width="match_parent"
        android:layout_height="160dp"
        android:hint="Enter ADHD coping plan here..."
        android:gravity="top"
        android:layout_marginTop="14dp"
        android:padding="12dp"
        android:textSize="16sp" />

    <Button
        android:id="@+id/btnSaveAndShare"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Save/Share Report"
        android:backgroundTint="@color/SoftBlue"
        android:layout_marginTop="24dp"
        android:padding="12dp" />
</LinearLayout>
